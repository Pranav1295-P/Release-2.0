# Frontend env — copy to .env for production builds
# In dev, Vite proxies /api to localhost:5000 (see vite.config.js), so you can leave this unset.
# For Vercel deploy, set this to your backend URL, e.g. https://api.your-domain.com
VITE_API_URL=https://your-backend.onrender.com/api
